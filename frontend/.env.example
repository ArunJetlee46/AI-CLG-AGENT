# Frontend build-time env. Unset (default) = same origin, /api/v1
# (vite dev proxy, nginx proxy, Netlify redirect all pass /api/* through).
# If set, it must include the full prefix: https://beru-backend.onrender.com/api/v1
VITE_API_URL=
